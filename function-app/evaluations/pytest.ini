[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    slow: tests que tardan mas de 1s
    integration: tests que requieren red (Foundry/Azure)
